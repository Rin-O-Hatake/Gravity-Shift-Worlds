using UnityEngine;

namespace Core.Scripts.LevelContoller
{
    public class NextLevelPortal : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
